# 1. 进入编译器所在的父目录
cd /home/gonglingwen/toolchain/x86_bin/
# 强制删除名为 dev 的文件（如果它是文件夹也会被删掉）
rm -rf dev

# 2. 进入引擎所在的父目录
cd /home/gonglingwen/toolchain/arm64_phone_bin/
rm -rf dev

# 3. 进入库文件所在的父目录
cd /home/gonglingwen/toolchain/dev_tool/
rm -rf dev


mkdir -p /home/gonglingwen/toolchain/x86_bin/dev
mkdir -p /home/gonglingwen/toolchain/arm64_phone_bin/dev
mkdir -p /home/gonglingwen/toolchain/dev_tool/dev


